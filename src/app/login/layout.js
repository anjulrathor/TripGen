export const metadata = {
  title: "Join TripGen | Secure Authentication",
  description: "Sign in to unlock personalized AI travel planning.",
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}
