export const metadata = {
  title: "Trending Destinations | TripGen AI",
  description: "Discover the world's most beautiful escapes.",
};

export default function DestinationsLayout({ children }) {
  return <>{children}</>;
}
