export const metadata = {
  title: "About Us | TripGen AI",
  description: "Learn about the mission behind the world's smartest travel planner.",
};

export default function AboutLayout({ children }) {
  return <>{children}</>;
}
